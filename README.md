## 📘 **README - Columnar Cipher**

### 📌 **О проекте**
Десктопное приложение на Wails для шифрования/дешифрования текста **столбцовым методом** с ключевым словом.

---

### 📁 **Структура**

```
main.go              # Точка входа
app.go               # ОСНОВНАЯ ЛОГИКА (шифрование, дешифрование, файлы)
frontend/App.jsx     # Интерфейс
```

---

### 🔐 **Где реализована основная логика**

**Весь криптоалгоритм находится в `app.go`:**

| Метод | Описание |
|-------|----------|
| `Encrypt(text, key)` | Шифрование |
| `Decrypt(text, key)` | Дешифрование |
| `getColOrder(key)` | Сортировка столбцов по ключу |
| `OpenFile()` | Загрузка из файла |
| `SaveFile()` | Сохранение в файл |

---

### 🛠 **Сборка**

```bash
wails build
```

Готовый `.exe` в `build/bin/`

---

### ✅ **Требования**
- Только английские буквы (A-Z)
- Ключевое слово (любой длины)
